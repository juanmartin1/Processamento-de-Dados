#include <stdio.h>

int main(){
	int a,b,c,d;
	printf("Digite os quatro numeros a serem somados\n");
	scanf("%d %d %d %d", &a, &b, &c, &d); 
	printf("\nA soma dos numeros é igual a: %d", a + b + c + d);
	return 0;
}
